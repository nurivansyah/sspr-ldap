services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: sspr-ldap:local
    env_file:
      - .env
    ports:
      - "${PORT:-8080}:${PORT:-8080}"
    restart: unless-stopped
